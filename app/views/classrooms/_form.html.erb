<%= form_with(model: classroom) do |form| %>
  <% if classroom.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
        <% classroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room, style: "display: block" %>
    <%= form.text_field :room %>
  </div>

  <div>
    <%= form.label :is_lab, style: "display: block" %>
    <%= form.number_field :is_lab %>
  </div>

  <div>
    <%= form.label :room_capacity, style: "display: block" %>
    <%= form.number_field :room_capacity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
